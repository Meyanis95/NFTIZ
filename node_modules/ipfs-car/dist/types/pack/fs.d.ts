import type { PackProperties } from './index';
export declare type PackToFsProperties = PackProperties & {
    input: string | Iterable<string> | AsyncIterable<string>;
    output?: string;
};
export declare function packToFs({ input, output, blockstore: userBlockstore, hasher, maxChunkSize, maxChildrenPerNode, wrapWithDirectory }: PackToFsProperties): Promise<{
    root: import("multiformats").CID;
    filename: string;
}>;
