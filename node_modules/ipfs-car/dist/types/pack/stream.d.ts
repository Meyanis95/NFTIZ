/// <reference types="node" />
import { Writable } from 'stream';
import type { PackProperties } from './index';
export declare type PackToStreamProperties = PackProperties & {
    input: string | Iterable<string> | AsyncIterable<string>;
    writable: Writable;
};
export declare function packToStream({ input, writable, blockstore: userBlockstore, hasher, maxChunkSize, maxChildrenPerNode, wrapWithDirectory }: PackToStreamProperties): Promise<{
    root: import("multiformats").CID;
}>;
