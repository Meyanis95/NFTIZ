import type { ImportCandidateStream } from 'ipfs-core-types/src/utils';
import type { MultihashHasher } from 'multiformats/hashes/interface';
export type { ImportCandidateStream };
import { Blockstore } from '../blockstore/index';
export declare type PackProperties = {
    input: ImportCandidateStream;
    blockstore?: Blockstore;
    maxChunkSize?: number;
    maxChildrenPerNode?: number;
    wrapWithDirectory?: boolean;
    hasher?: MultihashHasher;
};
export declare function pack({ input, blockstore: userBlockstore, hasher, maxChunkSize, maxChildrenPerNode, wrapWithDirectory }: PackProperties): Promise<{
    root: import("multiformats").CID;
    out: AsyncIterable<Uint8Array>;
}>;
